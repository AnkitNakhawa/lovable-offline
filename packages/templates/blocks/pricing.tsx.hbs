import React from 'react'

export default function {{componentName}}(props: any) {
return (
<section className="bg-white py-24">
    <div className="container mx-auto px-4">
        <h2 className="text-3xl font-bold text-center text-gray-900 mb-16">
            {{title}}
        </h2>
        <div className="grid grid-cols-1 md:grid-cols-3 gap-8 max-w-7xl mx-auto">
            {{#each plans}}
            <div
                className="h-full border border-gray-200 rounded-2xl p-8 hover:shadow-lg transition-shadow duration-300 flex flex-col bg-white">
                <h3 className="text-xl font-semibold text-gray-900 mb-4">{{this.name}}</h3>
                <div className="flex items-baseline mb-8">
                    <span className="text-4xl font-bold text-gray-900">{{this.price}}</span>
                    <span className="text-gray-500 ml-2">/month</span>
                </div>
                <ul className="space-y-4 mb-8 flex-1">
                    {{#each this.features}}
                    <li className="flex items-center text-gray-600">
                        <svg className="w-5 h-5 text-green-500 mr-2 flex-shrink-0" fill="none" stroke="currentColor"
                            viewBox="0 0 24 24">
                            <path strokeLinecap="round" strokeLinejoin="round" strokeWidth="2" d="M5 13l4 4L19 7" />
                        </svg>
                        <span>{{this}}</span>
                    </li>
                    {{/each}}
                </ul>
                <button
                    className="w-full bg-indigo-600 text-white font-semibold py-3 px-6 rounded-lg hover:bg-indigo-700 transition-colors mt-auto">
                    {{this.ctaText}}
                </button>
            </div>
            {{/each}}
        </div>
    </div>
</section>
)
}